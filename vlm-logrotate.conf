# Rotates the VLM sidecar's log; applied by the vlm-logrotate service.
# copytruncate is required: tee holds the file open (rename would orphan it).
/logs/*.log {
    daily
    rotate 7
    copytruncate
    compress
    delaycompress
    missingok
    notifempty
}
