# VLM model config for the vLLM sidecar (loaded via `env_file:` in
# docker-compose.yaml). Needs vllm/vllm-openai >= v0.23.0. 15.75B BF16 =
# ~32 GB of weights: 40 GB+ card, or 2x 24 GB with VLM_TENSOR_PARALLEL_SIZE=2.
VLM_MODEL=nvidia/Cosmos3-Nano
VLM_MAX_MODEL_LEN=32768
VLM_GPU_MEMORY_UTILIZATION=0.90
# Per-image pixel caps (Qwen-style processor kwargs; ~512 vision tokens/image).
VLM_MIN_PIXELS=3136
VLM_MAX_PIXELS=401408
# Uncomment to shard across two GPUs (pin them with VLM_GPU_IDS in .env):
# VLM_TENSOR_PARALLEL_SIZE=2
